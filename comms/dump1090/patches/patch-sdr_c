prevent abort() on closing with CTRL-C
Index: sdr.c
--- sdr.c.orig
+++ sdr.c
@@ -237,7 +237,6 @@ void sdrStop()
 void sdrClose()
 {
     pthread_mutex_destroy(&Modes.reader_cpu_mutex);
-    current_handler()->close();
 }
 
 void sdrMonitor()
